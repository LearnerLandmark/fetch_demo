Iterator design pattern is to “provide a way to access the elements of an aggregate object sequentially without exposing the underlying representation.”

C++ STL containers are implementing iterator design pattern, Iterator pattern purpose is to iterate over all elements of a container without having to worry about how the elements in the container are stored or retrieved or managed?
